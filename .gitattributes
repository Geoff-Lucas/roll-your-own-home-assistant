# Files that run on the Linux kiosk must keep LF endings even when checked
# out on Windows (core.autocrlf=true would otherwise turn them into CRLF and
# break them: "bad interpreter" / stray \r in unit files).
*.sh text eol=lf
deploy/xinitrc text eol=lf
*.desktop text eol=lf
*.service text eol=lf
